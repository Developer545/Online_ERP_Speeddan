{
  "version": 2,
  "buildCommand": "npx prisma migrate resolve --rolled-back 20260324000000_tipo_asiento_config || true && npx prisma migrate deploy && npx prisma generate && npx vite build --config vite.config.web.ts",
  "outputDirectory": "dist/renderer",
  "functions": {
    "api/index.ts": {
      "memory": 1024,
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
